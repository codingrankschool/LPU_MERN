//Internal Module

let mathOperations = {
  add: (a, b) => a + b,
  divide: (a, b) => a / b,
  subtract: (a, b) => a - b,
  multiply: (a, b) => a * b,
};


//Default Export
// module.exports = mathOperations;

//Named Export
module.exports = {mathOperations};
